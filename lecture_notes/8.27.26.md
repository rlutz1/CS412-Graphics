# 8.27.26

**Hw 1 up**
+ basically the play ground is the homework as a freebie
+ motivation: do something interesting.
+ obviously, make edits to the source code given, not just the playground, haha

preference is to host online your hw on like github pages at something. not required right now.

**thoughts on hw 1 ideas**

this is about as easy as it is to get 3 points: 
*except you also need to change the vertex shader to do something lol, don't just do this*

```
float alpha = sin(uTime);
fragColor = vec4(vColor, alpha); 
```

-> random check for this is, for example, is why is alpha the last parameter? (RGBA, baby)

"extra points for CREATIVITY -- hardest part of the class :)"

## point

**infinitely small unit**

P = (x)
P = (x, y)
P = (x, y, z)

given a point, its a context point in our coordinate system

## vector

**represent direction and magnitude**

vector can be a point if an origin

vector that's not a point, but related by
$v = p_2 - p_1$
+ direction of one point to the other.

1D = [x]
....
nD = [x_1, x_2, x_3, ...,x_n]

LOOK AT THE SLIDES FOR A REFERSH ON VECTOR AND MARTIX MATH REFRESH

## random comments made aside from the math refresh slides

matrices are great because
+ you can represent transformations of objects SUPER well with them
+ you can then throw them at the gpu for highly parallel processing

represent 2D:
3x3

represent 3D: 
4x4

represent 4D:
5x5 

TODO: *why n + 1 dimension?* because its a homogenous coordinate -- refresh this

adding matrics -> shit gets brighter

frame by frame difference -- **used for movement detection**
+ done with matrix subtraction

movement and rotation -- all matrix multiplacation, baby

when you see **orthogonal**, think *perspective*